RSpec.describe Animate::Css::Rails do
  it "has a version number" do
    expect(Animate::Css::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
